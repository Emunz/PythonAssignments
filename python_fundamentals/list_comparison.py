list_one = ['celery', 'bread', 'carrots', 'milk']
list_two = ['celery', 'bread', 'carrots', 'cream']

def list_comparison(x, y):
    if(x == y):
        print "These lists are the same"
    else:
        print "These lists are not the same"


list_comparison(list_one, list_two)